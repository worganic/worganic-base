import { Routes } from '@angular/router';

// Ajoutez ici les routes spécifiques à ce child.
// Ce fichier n'est jamais écrasé par les propagations de la base.
// Exemple :
// import { authGuard } from '../core/guards/auth.guard';
// export const CHILD_ROUTES: Routes = [
//   { path: 'projets', canActivate: [authGuard], loadComponent: () => import('./pages/projets/projets.component').then(m => m.ProjetsComponent) },
// ];

export const CHILD_ROUTES: Routes = [];
